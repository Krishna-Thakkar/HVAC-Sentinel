# HVAC Sentinel — Backend Dependencies
# Phase 0: Foundational setup only

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Data processing
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2

# Environment configuration
python-dotenv==1.0.1

# HTTP client (for AI API calls in later phases)
httpx==0.28.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx  # also used as async test client for FastAPI
